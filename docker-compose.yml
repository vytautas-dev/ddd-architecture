services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bidflow
      POSTGRES_PASSWORD: bidflow
      POSTGRES_DB: bidflow
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: